import React, { useState } from 'react';

const Tweets = () => {
  const [tweets, setTweets] = useState(null);
  return (
    <main>
      <div>Tweets</div>
    </main>
  );
};

export default Tweets;
